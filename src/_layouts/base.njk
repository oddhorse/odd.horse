<!DOCTYPE html>
<html lang="{{ meta.lang }}">
    {% include "head.njk" %}
    <body>
        {# put header for every page but index #}
        {% if page.url != "/" %}
            {% include "header.njk" %}
        {% endif %}
        <main>
            {{ content | safe }}
        </main>
        {# {% include "footer.njk" %} #}

        {# add-ins #}
        {% include "background.njk" %}
    </body>
</html>