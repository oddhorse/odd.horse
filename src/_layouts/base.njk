<!DOCTYPE html>
<html lang="{{ meta.lang }}">
    {% include "head.njk" %}
    <body>
        {# Beta menu - only shows on beta branch #}
        {% include "beta-menu.njk" %}

        {# put header for every page but index #}
        {% if page.url != "/" %}
            {% include "header.njk" %}
        {% endif %}
        <main>
            {{ content | safe }}
        </main>
        {# {% include "footer.njk" %} #}

        {# add-ins #}
        {# {% include "background.njk" %} #}
    </body>
</html>