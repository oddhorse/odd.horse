<!DOCTYPE html>
<html lang="{{ meta.lang }}">
    <head>
        {% include "head.njk" %} <style>:root
        {
            --page-color: {{pages[name].color}}
            ;
        }
    </style>
</head>
<body>
    {% include "header.njk" %}
    <main>
        {{ content | safe }}
    </main>
    {% include "footer.njk" %}
</body></html>