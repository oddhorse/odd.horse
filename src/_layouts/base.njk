<!DOCTYPE html>
<html lang="{{ meta.lang }}">
    {% include "head.njk" %}
    <body>
        {# put header for every page but index #}
        {% if page.url != "/" %}
            {% include "header.njk" %}
        {% endif %}
        <main>
            {{ content | safe }}
        </main>
        {% include "footer.njk" %}
    </body>
</html>